{
  "name": "koaserver",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "(cd frontend) && (npm run build) && (cd ..) && (gcloud app deploy app.yaml --quiet)",
    "start": "node index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@koa/cors": "^2.2.3",
    "koa": "^2.7.0",
    "koa-body": "^4.1.0",
    "koa-router": "^7.4.0",
    "pg": "^7.11.0",
    "slonik": "^16.19.5"
  }
}
